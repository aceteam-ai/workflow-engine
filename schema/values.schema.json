{
  "$comment": "Generated by scripts/generate_interchange_schema.py. See schema/README.md for the portability contract and validation limits.",
  "$defs": {
    "AbsoluteValueNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "AbsoluteValueNode",
      "type": "object"
    },
    "AddNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/AddNodeParams",
          "default": {
            "num_arguments": 2
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "AddNode",
      "type": "object"
    },
    "AddNodeParams": {
      "additionalProperties": false,
      "properties": {
        "num_arguments": {
          "$ref": "#/$defs/IntegerValue",
          "default": 2,
          "description": "The number of numbers to add.",
          "minimum": 2,
          "title": "Number of Arguments"
        }
      },
      "title": "AddNodeParams",
      "type": "object"
    },
    "AllOkNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "AllOkNode",
      "type": "object"
    },
    "AndNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/LogicalParams",
          "default": {
            "num_arguments": 2
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "AndNode",
      "type": "object"
    },
    "AppendToFileNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/AppendToFileParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "AppendToFileNode",
      "type": "object"
    },
    "AppendToFileParams": {
      "additionalProperties": false,
      "properties": {
        "suffix": {
          "$ref": "#/$defs/StringValue",
          "description": "The suffix to add to the output filename.",
          "title": "Suffix"
        }
      },
      "required": [
        "suffix"
      ],
      "title": "AppendToFileParams",
      "type": "object"
    },
    "AttemptNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/AttemptParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "AttemptNode",
      "type": "object"
    },
    "AttemptParams": {
      "additionalProperties": false,
      "properties": {
        "allow_metered": {
          "$ref": "#/$defs/BooleanValue",
          "default": false,
          "description": "The permission to repeat work that may incur an additional charge.",
          "title": "Allow Metered Retries"
        },
        "retries": {
          "$ref": "#/$defs/IntegerValue",
          "default": 0,
          "description": "The maximum number of additional runs after a matching failure.",
          "minimum": 0,
          "title": "Retries"
        },
        "retry_on": {
          "$ref": "#/$defs/SequenceValue_ErrorClassValue_",
          "default": [
            "timeout",
            "unreachable",
            "rate_limit"
          ],
          "description": "The error classes that permit another run of the workflow.",
          "title": "Retry On"
        },
        "workflow": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run inside the error boundary.",
          "title": "Workflow"
        }
      },
      "required": [
        "workflow"
      ],
      "title": "AttemptParams",
      "type": "object"
    },
    "AttemptRetryNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/AttemptRetryParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "AttemptRetryNode",
      "type": "object"
    },
    "AttemptRetryParams": {
      "additionalProperties": false,
      "properties": {
        "allow_metered": {
          "$ref": "#/$defs/BooleanValue",
          "default": false,
          "description": "The permission to repeat work that may incur an additional charge.",
          "title": "Allow Metered Retries"
        },
        "boundary_id": {
          "$ref": "#/$defs/StringValue",
          "description": "The original attempt node's identifier.",
          "title": "Boundary ID"
        },
        "completed_attempt": {
          "$ref": "#/$defs/IntegerValue",
          "description": "The zero-based number of the attempt whose result is being checked.",
          "title": "Completed Attempt"
        },
        "retries": {
          "$ref": "#/$defs/IntegerValue",
          "default": 0,
          "description": "The maximum number of additional runs after a matching failure.",
          "minimum": 0,
          "title": "Retries"
        },
        "retry_on": {
          "$ref": "#/$defs/SequenceValue_ErrorClassValue_",
          "default": [
            "timeout",
            "unreachable",
            "rate_limit"
          ],
          "description": "The error classes that permit another run of the workflow.",
          "title": "Retry On"
        },
        "workflow": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run inside the error boundary.",
          "title": "Workflow"
        }
      },
      "required": [
        "workflow",
        "boundary_id",
        "completed_attempt"
      ],
      "title": "AttemptRetryParams",
      "type": "object"
    },
    "BaseValueSchema": {
      "additionalProperties": false,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "x-value-type": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "x-value-type"
      ],
      "title": "BaseValueSchema",
      "type": "object"
    },
    "BooleanValue": {
      "title": "BooleanValue",
      "type": "boolean",
      "x-value-type": "BooleanValue"
    },
    "BooleanValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "boolean",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "BooleanValueSchema",
      "type": "object"
    },
    "CSVFileValue": {
      "$ref": "#/$defs/File",
      "title": "CSVFileValue",
      "x-value-type": "CSVFileValue"
    },
    "ChunkSequenceNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ChunkSequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ChunkSequenceNode",
      "type": "object"
    },
    "ChunkSequenceParams": {
      "additionalProperties": false,
      "properties": {
        "element_schema": {
          "$ref": "#/$defs/ValueSchemaValue",
          "description": "The value schema of each sequence item.",
          "title": "Element Schema"
        },
        "size": {
          "$ref": "#/$defs/IntegerValue",
          "description": "The maximum number of items in each chunk.",
          "title": "Size"
        }
      },
      "required": [
        "element_schema",
        "size"
      ],
      "title": "ChunkSequenceParams",
      "type": "object"
    },
    "ComparisonParams": {
      "additionalProperties": false,
      "properties": {},
      "title": "ComparisonParams",
      "type": "object"
    },
    "ConstantBoolean": {
      "additionalProperties": false,
      "properties": {
        "value": {
          "$ref": "#/$defs/BooleanValue",
          "description": "The constant boolean value.",
          "title": "Value"
        }
      },
      "required": [
        "value"
      ],
      "title": "ConstantBoolean",
      "type": "object"
    },
    "ConstantBooleanNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ConstantBoolean",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ConstantBooleanNode",
      "type": "object"
    },
    "ConstantInteger": {
      "additionalProperties": false,
      "properties": {
        "value": {
          "$ref": "#/$defs/IntegerValue",
          "description": "The constant integer value.",
          "title": "Value"
        }
      },
      "required": [
        "value"
      ],
      "title": "ConstantInteger",
      "type": "object"
    },
    "ConstantIntegerNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ConstantInteger",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ConstantIntegerNode",
      "type": "object"
    },
    "ConstantString": {
      "additionalProperties": false,
      "properties": {
        "value": {
          "$ref": "#/$defs/StringValue",
          "description": "The constant string value.",
          "title": "Value"
        }
      },
      "required": [
        "value"
      ],
      "title": "ConstantString",
      "type": "object"
    },
    "ConstantStringNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ConstantString",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ConstantStringNode",
      "type": "object"
    },
    "DataValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "additionalProperties": {
          "anyOf": [
            {
              "$ref": "#/$defs/ValueSchema"
            },
            {
              "type": "boolean"
            }
          ],
          "default": false,
          "title": "Additionalproperties"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "properties": {
          "additionalProperties": {
            "$ref": "#/$defs/ValueSchema"
          },
          "title": "Properties",
          "type": "object"
        },
        "required": {
          "items": {
            "type": "string"
          },
          "title": "Required",
          "type": "array"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "object",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type",
        "properties"
      ],
      "title": "DataValueSchema",
      "type": "object"
    },
    "DateValue": {
      "title": "DateValue",
      "type": "string",
      "x-value-type": "DateValue"
    },
    "DateValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "format": {
          "const": "date-time",
          "title": "Format",
          "type": "string"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "string",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type",
        "format"
      ],
      "title": "DateValueSchema",
      "type": "object"
    },
    "DivideNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/DivideParams",
          "default": {
            "rounding_mode": "down"
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "DivideNode",
      "type": "object"
    },
    "DivideParams": {
      "additionalProperties": false,
      "properties": {
        "rounding_mode": {
          "$ref": "#/$defs/RoundingModeValue",
          "default": "down",
          "description": "The rounding rule applied to the exact quotient to produce the integer quotient. The remainder is computed from this rounded integer quotient.",
          "title": "Rounding Mode"
        }
      },
      "title": "DivideParams",
      "type": "object"
    },
    "DocXFileValue": {
      "$ref": "#/$defs/File",
      "title": "DocXFileValue",
      "x-value-type": "DocXFileValue"
    },
    "Edge": {
      "properties": {
        "source_id": {
          "title": "Source Id",
          "type": "string"
        },
        "source_key": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            }
          ],
          "title": "Source Key"
        },
        "target_id": {
          "title": "Target Id",
          "type": "string"
        },
        "target_key": {
          "minLength": 1,
          "title": "Target Key",
          "type": "string"
        }
      },
      "required": [
        "source_id",
        "source_key",
        "target_id",
        "target_key"
      ],
      "title": "Edge",
      "type": "object"
    },
    "ElementParams": {
      "additionalProperties": false,
      "properties": {
        "element_schema": {
          "$ref": "#/$defs/ValueSchemaValue",
          "description": "The value schema of each sequence item.",
          "title": "Element Schema"
        }
      },
      "required": [
        "element_schema"
      ],
      "title": "ElementParams",
      "type": "object"
    },
    "Empty": {
      "additionalProperties": false,
      "properties": {},
      "title": "Empty",
      "type": "object"
    },
    "EntriesNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ElementParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "EntriesNode",
      "type": "object"
    },
    "EqualNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/EqualityParams",
          "default": {
            "abs_tol": 0.0,
            "rel_tol": 0.0
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "EqualNode",
      "type": "object"
    },
    "EqualityParams": {
      "additionalProperties": false,
      "properties": {
        "abs_tol": {
          "$ref": "#/$defs/FloatValue",
          "default": 0.0,
          "description": "The maximum absolute difference between the two values for them to count as equal. Useful when comparing values near zero, where the relative tolerance is too strict.",
          "minimum": 0,
          "title": "Absolute Tolerance"
        },
        "rel_tol": {
          "$ref": "#/$defs/FloatValue",
          "default": 0.0,
          "description": "The maximum difference between the two values relative to the larger of their magnitudes, for them to count as equal. Use this to absorb floating-point rounding error. Defaults to 0, meaning an exact comparison.",
          "minimum": 0,
          "title": "Relative Tolerance"
        }
      },
      "title": "EqualityParams",
      "type": "object"
    },
    "ErrorClass": {
      "description": "The closed-vocabulary, machine-readable classification of an error.\n\nA single field lets callers key retry policy, circuit breakers, and a run\nledger off of one vocabulary instead of three. Defined here (rather than\nin ``core/values/result.py``, where it originated) because\n``WorkflowException`` needs it and ``core/error.py`` must not import\n``core/values``; ``values/result.py`` re-exports it so the wire shape and\npublic import path are unchanged.",
      "enum": [
        "timeout",
        "unreachable",
        "rate_limit",
        "validation",
        "permission",
        "systemic"
      ],
      "title": "ErrorClass",
      "type": "string"
    },
    "ErrorClassValue": {
      "$ref": "#/$defs/ErrorClass",
      "title": "ErrorClassValue",
      "x-value-type": "ErrorClassValue"
    },
    "ErrorNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ErrorParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ErrorNode",
      "type": "object"
    },
    "ErrorParams": {
      "additionalProperties": false,
      "properties": {
        "error_name": {
          "$ref": "#/$defs/StringValue",
          "description": "The name of the error to raise.",
          "title": "Error Name"
        }
      },
      "required": [
        "error_name"
      ],
      "title": "ErrorParams",
      "type": "object"
    },
    "ExpandDataNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "ExpandDataNode",
      "type": "object"
    },
    "ExpandMappingNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/MappingParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "value_type": {
          "$ref": "#/$defs/ValueType"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ExpandMappingNode",
      "type": "object"
    },
    "ExpandSequenceNode": {
      "additionalProperties": true,
      "properties": {
        "element_type": {
          "$ref": "#/$defs/ValueType"
        },
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/SequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ExpandSequenceNode",
      "type": "object"
    },
    "FactorizationNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "FactorizationNode",
      "type": "object"
    },
    "FieldSchemaMappingValue": {
      "additionalProperties": {
        "$ref": "#/$defs/ValueSchemaValue"
      },
      "title": "FieldSchemaMappingValue",
      "type": "object",
      "x-value-type": "FieldSchemaMappingValue"
    },
    "File": {
      "additionalProperties": false,
      "properties": {
        "metadata": {
          "additionalProperties": true,
          "title": "Metadata",
          "type": "object"
        },
        "path": {
          "title": "Path",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "title": "File",
      "type": "object"
    },
    "FileValue": {
      "$ref": "#/$defs/File",
      "title": "FileValue",
      "x-value-type": "FileValue"
    },
    "FilterNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/WorkflowSequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "FilterNode",
      "type": "object"
    },
    "FirstErrorNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "FirstErrorNode",
      "type": "object"
    },
    "FlattenSequenceNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ElementParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "FlattenSequenceNode",
      "type": "object"
    },
    "FloatValue": {
      "title": "FloatValue",
      "type": "number",
      "x-value-type": "FloatValue"
    },
    "FloatValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "number",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "FloatValueSchema",
      "type": "object"
    },
    "FoldNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/WorkflowSequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "FoldNode",
      "type": "object"
    },
    "FoldStepNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/WorkflowSequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "FoldStepNode",
      "type": "object"
    },
    "ForEachNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ForEachParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ForEachNode",
      "type": "object"
    },
    "ForEachParams": {
      "additionalProperties": false,
      "properties": {
        "constant_inputs": {
          "$ref": "#/$defs/SequenceValue_StringValue_",
          "default": [],
          "description": "The workflow inputs held constant for every item.",
          "title": "Constant Inputs"
        },
        "workflow": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run for each item.",
          "title": "Workflow"
        }
      },
      "required": [
        "workflow"
      ],
      "title": "ForEachParams",
      "type": "object"
    },
    "GatherDataNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "GatherDataNode",
      "type": "object"
    },
    "GatherMappingNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/MappingParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "value_type": {
          "$ref": "#/$defs/ValueType"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "GatherMappingNode",
      "type": "object"
    },
    "GatherSequenceNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/SequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "GatherSequenceNode",
      "type": "object"
    },
    "GreaterThanEqualNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ComparisonParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "GreaterThanEqualNode",
      "type": "object"
    },
    "GreaterThanNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ComparisonParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "GreaterThanNode",
      "type": "object"
    },
    "GroupByNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/WorkflowSequenceParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "GroupByNode",
      "type": "object"
    },
    "GroupSequenceNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ElementParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "GroupSequenceNode",
      "type": "object"
    },
    "Hints": {
      "additionalProperties": true,
      "properties": {
        "max_concurrency": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A suggested upper bound on how many of this node's parallel branches (for example, the items of a For Each) a host should run at once. A host may clamp this lower to protect its own resources, run fewer branches at a time, or ignore it entirely and run unbounded; the workflow's result does not depend on how many branches were in flight at once.",
          "title": "Max Concurrency"
        }
      },
      "title": "Hints",
      "type": "object"
    },
    "IfElseNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/IfElseParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "IfElseNode",
      "type": "object"
    },
    "IfElseParams": {
      "additionalProperties": false,
      "properties": {
        "if_false": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run when the condition is false.",
          "title": "If False"
        },
        "if_true": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run when the condition is true.",
          "title": "If True"
        }
      },
      "required": [
        "if_true",
        "if_false"
      ],
      "title": "IfElseParams",
      "type": "object"
    },
    "IfNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/IfParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "IfNode",
      "type": "object"
    },
    "IfParams": {
      "additionalProperties": false,
      "properties": {
        "if_true": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run when the condition is true.",
          "title": "If True"
        }
      },
      "required": [
        "if_true"
      ],
      "title": "IfParams",
      "type": "object"
    },
    "InputNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/SchemaParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "InputNode",
      "type": "object"
    },
    "IntegerValue": {
      "title": "IntegerValue",
      "type": "integer",
      "x-value-type": "IntegerValue"
    },
    "IntegerValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "integer",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "IntegerValueSchema",
      "type": "object"
    },
    "IsOkNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "IsOkNode",
      "type": "object"
    },
    "JSON": {
      "anyOf": [
        {
          "additionalProperties": {
            "$ref": "#/$defs/JSON"
          },
          "type": "object"
        },
        {
          "items": {
            "$ref": "#/$defs/JSON"
          },
          "type": "array"
        },
        {
          "type": "boolean"
        },
        {
          "type": "integer"
        },
        {
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "type": "string"
        },
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "JSONFileValue": {
      "$ref": "#/$defs/File",
      "title": "JSONFileValue",
      "x-value-type": "JSONFileValue"
    },
    "JSONLinesFileValue": {
      "$ref": "#/$defs/File",
      "title": "JSONLinesFileValue",
      "x-value-type": "JSONLinesFileValue"
    },
    "JSONValue": {
      "$ref": "#/$defs/JSON",
      "title": "JSONValue",
      "x-value-type": "JSONValue"
    },
    "LessThanEqualNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ComparisonParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "LessThanEqualNode",
      "type": "object"
    },
    "LessThanNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ComparisonParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "LessThanNode",
      "type": "object"
    },
    "LogicalParams": {
      "additionalProperties": false,
      "properties": {
        "num_arguments": {
          "$ref": "#/$defs/IntegerValue",
          "default": 2,
          "description": "The number of boolean inputs to combine.",
          "minimum": 2,
          "title": "Number of Arguments"
        }
      },
      "title": "LogicalParams",
      "type": "object"
    },
    "MappingParams": {
      "additionalProperties": false,
      "properties": {
        "keys": {
          "$ref": "#/$defs/SequenceValue_StringValue_",
          "description": "The keys of the mapping.",
          "title": "Keys"
        }
      },
      "required": [
        "keys"
      ],
      "title": "MappingParams",
      "type": "object"
    },
    "MarkdownFileValue": {
      "$ref": "#/$defs/File",
      "title": "MarkdownFileValue",
      "x-value-type": "MarkdownFileValue"
    },
    "MatchErrorClassNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/MatchErrorClassParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "MatchErrorClassNode",
      "type": "object"
    },
    "MatchErrorClassParams": {
      "additionalProperties": false,
      "properties": {
        "branches": {
          "$ref": "#/$defs/StringMapValue_WorkflowValue_",
          "description": "The sub-workflow to run for each error_class value, keyed by the ErrorClass string (e.g. 'timeout'). Must have exactly one entry per value ErrorClass currently defines: graph validation rejects a node whose branches do not cover every value.",
          "title": "Branches"
        }
      },
      "required": [
        "branches"
      ],
      "title": "MatchErrorClassParams",
      "type": "object"
    },
    "MaximumNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "MaximumNode",
      "type": "object"
    },
    "MinimumNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "MinimumNode",
      "type": "object"
    },
    "MultiplyNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "MultiplyNode",
      "type": "object"
    },
    "NegateNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "NegateNode",
      "type": "object"
    },
    "Node": {
      "additionalProperties": true,
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "AbsoluteValue"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AbsoluteValueNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Add"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AddNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "AllOk"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AllOkNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "And"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AndNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "AppendToFile"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AppendToFileNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Attempt"
              },
              "version": {
                "const": "1.1.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AttemptNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "AttemptRetry"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/AttemptRetryNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ChunkSequence"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ChunkSequenceNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ConstantBoolean"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ConstantBooleanNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ConstantInteger"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ConstantIntegerNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ConstantString"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ConstantStringNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Divide"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/DivideNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Entries"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/EntriesNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Equal"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/EqualNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Error"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ErrorNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ExpandData"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ExpandDataNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ExpandMapping"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ExpandMappingNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ExpandSequence"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ExpandSequenceNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Factorization"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/FactorizationNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Filter"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/FilterNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "FirstError"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/FirstErrorNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "FlattenSequence"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/FlattenSequenceNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Fold"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/FoldNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "FoldStep"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/FoldStepNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "ForEach"
              },
              "version": {
                "const": "1.1.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ForEachNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GatherData"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GatherDataNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GatherMapping"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GatherMappingNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GatherSequence"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GatherSequenceNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GreaterThan"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GreaterThanNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GreaterThanEqual"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GreaterThanEqualNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GroupBy"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GroupByNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "GroupSequence"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/GroupSequenceNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "If"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/IfNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "IfElse"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/IfElseNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Input"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/InputNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "IsOk"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/IsOkNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "LessThan"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/LessThanNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "LessThanEqual"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/LessThanEqualNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "MatchErrorClass"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/MatchErrorClassNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Maximum"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/MaximumNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Minimum"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/MinimumNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Multiply"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/MultiplyNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Negate"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/NegateNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Not"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/NotNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "NotEqual"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/NotEqualNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Now"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/NowNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Ok"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/OkNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Or"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/OrNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Output"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/OutputNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Partition"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/PartitionNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Power"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/PowerNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Round"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/RoundNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "SelectSequence"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/SelectSequenceNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Subtract"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/SubtractNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Sum"
              },
              "version": {
                "const": "0.4.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/SumNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Unwrap"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/UnwrapNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "UnwrapOr"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/UnwrapOrNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "UnwrapOrValue"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/UnwrapOrValueNode"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "Zip"
              },
              "version": {
                "const": "1.0.0"
              }
            },
            "required": [
              "type",
              "version"
            ]
          },
          "then": {
            "$ref": "#/$defs/ZipNode"
          }
        }
      ],
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "type": "object"
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "Node",
      "type": "object"
    },
    "NotEqualNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/EqualityParams",
          "default": {
            "abs_tol": 0.0,
            "rel_tol": 0.0
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "NotEqualNode",
      "type": "object"
    },
    "NotNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "NotNode",
      "type": "object"
    },
    "NowNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "NowNode",
      "type": "object"
    },
    "NullValue": {
      "title": "NullValue",
      "type": "null",
      "x-value-type": "NullValue"
    },
    "NullValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "null",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "NullValueSchema",
      "type": "object"
    },
    "OkNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "OkNode",
      "type": "object"
    },
    "OrNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/LogicalParams",
          "default": {
            "num_arguments": 2
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "OrNode",
      "type": "object"
    },
    "OutputNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/SchemaParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "OutputNode",
      "type": "object"
    },
    "PDFFileValue": {
      "$ref": "#/$defs/File",
      "title": "PDFFileValue",
      "x-value-type": "PDFFileValue"
    },
    "PartitionNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "PartitionNode",
      "type": "object"
    },
    "PowerNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "PowerNode",
      "type": "object"
    },
    "ReferenceValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "$ref": {
          "pattern": "^#/\\$defs/\\w+$",
          "title": "$Ref",
          "type": "string"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "$ref"
      ],
      "title": "ReferenceValueSchema",
      "type": "object"
    },
    "ResultValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "err": {
          "$ref": "#/$defs/ValueSchema"
        },
        "ok": {
          "$ref": "#/$defs/ValueSchema"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "object",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type",
        "ok",
        "err"
      ],
      "title": "ResultValueSchema",
      "type": "object"
    },
    "RoundNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/RoundParams",
          "default": {
            "digits": 0,
            "rounding_mode": "half_even"
          }
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "RoundNode",
      "type": "object"
    },
    "RoundParams": {
      "additionalProperties": false,
      "properties": {
        "digits": {
          "$ref": "#/$defs/IntegerValue",
          "default": 0,
          "description": "The number of decimal places to round to.",
          "title": "Decimal Places"
        },
        "rounding_mode": {
          "$ref": "#/$defs/RoundingModeValue",
          "default": "half_even",
          "description": "The rounding rule applied when reducing the input value to the specified number of decimal places.",
          "title": "Rounding Mode"
        }
      },
      "title": "RoundParams",
      "type": "object"
    },
    "RoundingMode": {
      "description": "The rounding rule to apply.\n\nDirected modes: \"down\", \"up\", \"toward_zero\", \"away_from_zero\".\nNearest modes: \"half_even\", \"half_odd\", \"half_toward_zero\",\n\"half_away_from_zero\", \"half_up\", \"half_down\".",
      "enum": [
        "down",
        "up",
        "toward_zero",
        "away_from_zero",
        "half_toward_zero",
        "half_away_from_zero",
        "half_even",
        "half_odd",
        "half_up",
        "half_down"
      ],
      "title": "RoundingMode",
      "type": "string"
    },
    "RoundingModeValue": {
      "$ref": "#/$defs/RoundingMode",
      "title": "RoundingModeValue",
      "x-value-type": "RoundingModeValue"
    },
    "SchemaParams": {
      "additionalProperties": false,
      "properties": {
        "fields": {
          "$ref": "#/$defs/FieldSchemaMappingValue",
          "description": "A mapping of field names to value schemas.",
          "title": "Fields"
        }
      },
      "title": "SchemaParams",
      "type": "object"
    },
    "SelectSequenceNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ElementParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "SelectSequenceNode",
      "type": "object"
    },
    "SequenceParams": {
      "additionalProperties": false,
      "properties": {
        "length": {
          "$ref": "#/$defs/IntegerValue",
          "description": "The number of elements in the sequence.",
          "title": "Length"
        }
      },
      "required": [
        "length"
      ],
      "title": "SequenceParams",
      "type": "object"
    },
    "SequenceValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "items": {
          "anyOf": [
            {
              "$ref": "#/$defs/ValueSchema"
            },
            {
              "const": true,
              "type": "boolean"
            }
          ],
          "default": true,
          "title": "Items"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "array",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "SequenceValueSchema",
      "type": "object"
    },
    "SequenceValue_ErrorClassValue_": {
      "items": {
        "$ref": "#/$defs/ErrorClassValue"
      },
      "title": "SequenceValue[ErrorClassValue]",
      "type": "array",
      "x-value-type": "SequenceValue[ErrorClassValue]"
    },
    "SequenceValue_StringValue_": {
      "items": {
        "$ref": "#/$defs/StringValue"
      },
      "title": "SequenceValue[StringValue]",
      "type": "array",
      "x-value-type": "SequenceValue[StringValue]"
    },
    "StringMapValueSchema": {
      "additionalProperties": true,
      "not": {
        "anyOf": [
          {
            "required": [
              "properties"
            ]
          },
          {
            "required": [
              "ok"
            ]
          },
          {
            "required": [
              "err"
            ]
          }
        ]
      },
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "additionalProperties": {
          "anyOf": [
            {
              "$ref": "#/$defs/ValueSchema"
            },
            {
              "const": true,
              "type": "boolean"
            }
          ],
          "default": true,
          "title": "Additionalproperties"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "object",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "StringMapValueSchema",
      "type": "object"
    },
    "StringMapValue_WorkflowValue_": {
      "additionalProperties": {
        "$ref": "#/$defs/WorkflowValue"
      },
      "title": "StringMapValue[WorkflowValue]",
      "type": "object",
      "x-value-type": "StringMapValue[WorkflowValue]"
    },
    "StringValue": {
      "title": "StringValue",
      "type": "string",
      "x-value-type": "StringValue"
    },
    "StringValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "enum": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Enum"
        },
        "pattern": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Pattern"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "type": {
          "const": "string",
          "title": "Type",
          "type": "string"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "type"
      ],
      "title": "StringValueSchema",
      "type": "object"
    },
    "SubtractNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "SubtractNode",
      "type": "object"
    },
    "SumNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "SumNode",
      "type": "object"
    },
    "TextFileValue": {
      "$ref": "#/$defs/File",
      "title": "TextFileValue",
      "x-value-type": "TextFileValue"
    },
    "UnionValueSchema": {
      "additionalProperties": true,
      "properties": {
        "$defs": {
          "additionalProperties": {
            "type": "object"
          },
          "type": "object"
        },
        "anyOf": {
          "items": {
            "$ref": "#/$defs/ValueSchema"
          },
          "minItems": 1,
          "title": "Anyof",
          "type": "array"
        },
        "const": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Const"
        },
        "default": {
          "anyOf": [
            {},
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Default"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        },
        "x-value-type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Value Type"
        }
      },
      "required": [
        "anyOf"
      ],
      "title": "UnionValueSchema",
      "type": "object"
    },
    "UnwrapNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "UnwrapNode",
      "type": "object"
    },
    "UnwrapOrNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "UnwrapOrNode",
      "type": "object"
    },
    "UnwrapOrValueNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/Empty",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "version"
      ],
      "title": "UnwrapOrValueNode",
      "type": "object"
    },
    "ValueSchema": {
      "anyOf": [
        {
          "$ref": "#/$defs/BooleanValueSchema"
        },
        {
          "$ref": "#/$defs/DataValueSchema"
        },
        {
          "$ref": "#/$defs/DateValueSchema"
        },
        {
          "$ref": "#/$defs/FloatValueSchema"
        },
        {
          "$ref": "#/$defs/IntegerValueSchema"
        },
        {
          "$ref": "#/$defs/NullValueSchema"
        },
        {
          "$ref": "#/$defs/ResultValueSchema"
        },
        {
          "$ref": "#/$defs/SequenceValueSchema"
        },
        {
          "$ref": "#/$defs/StringMapValueSchema"
        },
        {
          "$ref": "#/$defs/StringValueSchema"
        },
        {
          "$ref": "#/$defs/UnionValueSchema"
        },
        {
          "$ref": "#/$defs/ReferenceValueSchema"
        },
        {
          "$ref": "#/$defs/BaseValueSchema"
        }
      ]
    },
    "ValueSchemaValue": {
      "$ref": "#/$defs/ValueSchema",
      "title": "ValueSchemaValue",
      "x-value-type": "ValueSchemaValue"
    },
    "ValueType": {},
    "Workflow": {
      "properties": {
        "edges": {
          "items": {
            "$ref": "#/$defs/Edge"
          },
          "title": "Edges",
          "type": "array"
        },
        "inner_nodes": {
          "items": {
            "$ref": "#/$defs/Node"
          },
          "title": "Inner Nodes",
          "type": "array"
        },
        "input_node": {
          "$ref": "#/$defs/InputNode"
        },
        "output_node": {
          "$ref": "#/$defs/OutputNode"
        }
      },
      "required": [
        "input_node",
        "inner_nodes",
        "output_node",
        "edges"
      ],
      "title": "Workflow",
      "type": "object"
    },
    "WorkflowSequenceParams": {
      "additionalProperties": false,
      "properties": {
        "workflow": {
          "$ref": "#/$defs/WorkflowValue",
          "description": "The workflow to run for each item.",
          "title": "Workflow"
        }
      },
      "required": [
        "workflow"
      ],
      "title": "WorkflowSequenceParams",
      "type": "object"
    },
    "WorkflowValue": {
      "$ref": "#/$defs/Workflow",
      "title": "WorkflowValue",
      "x-value-type": "WorkflowValue"
    },
    "ZipNode": {
      "additionalProperties": true,
      "properties": {
        "hints": {
          "$ref": "#/$defs/Hints",
          "description": "Host-facing annotations for this node. A host may honor, clamp, or ignore any hint; unlike params, hints never affect the node's input/output types or the workflow's result."
        },
        "id": {
          "description": "The ID of the node, which must be unique within the workflow.",
          "title": "Id",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/ZipParams",
          "description": "Any parameters for the node which are independent of the workflow inputs. May affect what inputs are accepted by the node."
        },
        "type": {
          "description": "The type of the node, which should be a literal string for discriminating concrete subclasses. Used to determine which node class to load.",
          "title": "Type",
          "type": "string"
        },
        "version": {
          "description": "The resolved node version. Export must pin latest before sharing.",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        }
      },
      "required": [
        "id",
        "params",
        "type",
        "version"
      ],
      "title": "ZipNode",
      "type": "object"
    },
    "ZipParams": {
      "additionalProperties": false,
      "properties": {
        "first_schema": {
          "$ref": "#/$defs/ValueSchemaValue",
          "description": "The value schema of each item in the first sequence.",
          "title": "First Schema"
        },
        "second_schema": {
          "$ref": "#/$defs/ValueSchemaValue",
          "description": "The value schema of each item in the second sequence.",
          "title": "Second Schema"
        }
      },
      "required": [
        "first_schema",
        "second_schema"
      ],
      "title": "ZipParams",
      "type": "object"
    }
  },
  "$id": "https://raw.githubusercontent.com/aceteam-ai/workflow-engine/main/schema/values.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "anyOf": [
    {
      "$ref": "#/$defs/BooleanValue"
    },
    {
      "$ref": "#/$defs/CSVFileValue"
    },
    {
      "$ref": "#/$defs/DateValue"
    },
    {
      "$ref": "#/$defs/DocXFileValue"
    },
    {
      "$ref": "#/$defs/ErrorClassValue"
    },
    {
      "$ref": "#/$defs/FieldSchemaMappingValue"
    },
    {
      "$ref": "#/$defs/FileValue"
    },
    {
      "$ref": "#/$defs/FloatValue"
    },
    {
      "$ref": "#/$defs/IntegerValue"
    },
    {
      "$ref": "#/$defs/JSONFileValue"
    },
    {
      "$ref": "#/$defs/JSONLinesFileValue"
    },
    {
      "$ref": "#/$defs/JSONValue"
    },
    {
      "$ref": "#/$defs/MarkdownFileValue"
    },
    {
      "$ref": "#/$defs/NullValue"
    },
    {
      "$ref": "#/$defs/PDFFileValue"
    },
    {
      "$ref": "#/$defs/RoundingModeValue"
    },
    {
      "$ref": "#/$defs/StringValue"
    },
    {
      "$ref": "#/$defs/TextFileValue"
    },
    {
      "$ref": "#/$defs/ValueSchemaValue"
    },
    {
      "$ref": "#/$defs/WorkflowValue"
    }
  ],
  "title": "Registered Value instances (select a named definition)"
}
